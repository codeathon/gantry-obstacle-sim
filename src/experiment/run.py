"""Full hunt entry. Why: pylon-track arena_experiment analogue."""


def main() -> None:
	# Why: open_gantry + AceCamera + Experiment.run; only place that binds both.
	raise NotImplementedError("experiment run main")


if __name__ == "__main__":
	main()
