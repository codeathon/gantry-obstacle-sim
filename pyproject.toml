[project]
name = "prey-gantry"
version = "0.1.0"
description = "Timed hunt simulation: pylon-track arena + Basler delay model + Zaber XY API"
requires-python = ">=3.11"
dependencies = [
	"fastapi>=0.115",
	"uvicorn[standard]>=0.32",
	"pydantic>=2.0",
]

[project.optional-dependencies]
zaber = ["zaber-motion>=7.0"]
pylon = ["pypylon>=3.0", "numpy>=1.26"]
dev = ["pytest>=8.0"]

[project.scripts]
prey-gantry-sim = "simulation.web.app:main"
prey-gantry-grab = "experiment.grab_frames:main"

[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
simulation = ["web/static/*"]

[tool.pytest.ini_options]
testpaths = ["test"]
pythonpath = ["src"]
